// pdf.js — Lazy-loaded PDF export functions
// Dynamic import pattern: window.loadPdfLibs() → then exportAchatsPDF / exportPrepPDF

// exportAchatsPDF and exportPrepPDF are defined in trail-redesigned.html.
// jsPDF + autotable are loaded lazily via loadPdfLibs().

export { };
