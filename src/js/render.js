// render.js — DOM rendering functions
// Exports: renderAchats, renderPrep, renderTrekSelector, renderCatIcon

// renderAchats, renderPrep, renderTrekSelector are defined inline in
// trail-redesigned.html (monolith) and exposed on window for backward compat.
// This module will house the extracted versions when architecture is split.

export { };
